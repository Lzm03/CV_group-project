[pytest]
testpaths = tests
addopts =
    -q
    -p no:cacheprovider
    --cov=config
    --cov=geometry
    --cov=guidance
    --cov=nlu
    --cov=pipeline
    --cov-report=term-missing
    --cov-fail-under=80
